export interface Book {
  name: string;
  condition: string;
  points: string;
  description: string;
  added: Date;
  wishlist: string[];
  ordered: number;
  pages: string;
  author: string;
  image: string;
}
