export interface Book {
  name: string;
  condition: string;
  points: string;
  description: string;
  added: Date;
  wishlist: string;
  ordered: string;
  pages: string;
  author: string;
  image: string;
}
